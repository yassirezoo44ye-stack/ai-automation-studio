# Development and testing dependencies — NOT installed in production
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0      # coverage measurement (pytest --cov=app)
httpx>=0.27          # async test client for FastAPI (future integration tests)
ruff==0.15.21        # linter — pinned: 0.16.0 changed its default rule
                     # selection and turned the existing 104-error ratchet
                     # baseline into 2197 (confirmed by installing 0.16.0
                     # locally and re-running the exact CI check against
                     # unchanged code). An unbounded floor (">=0.5") meant
                     # CI silently got whatever the latest release was on
                     # any given day; pinning restores CI/local parity.
                     # Bumping this is a deliberate, separate decision —
                     # not something that should happen as a side effect
                     # of a routine `pip install`.
pip-audit>=2.7       # dependency vulnerability scanner (CI)
