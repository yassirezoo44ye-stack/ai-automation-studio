# Development and testing dependencies — NOT installed in production
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0      # coverage measurement (pytest --cov=app)
httpx>=0.27          # async test client for FastAPI (future integration tests)
ruff>=0.5            # linter
pip-audit>=2.7       # dependency vulnerability scanner (CI)
